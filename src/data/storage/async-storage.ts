import AsyncStorage from '@react-native-async-storage/async-storage';

/** Persistent storage supported by Expo Go and Supabase Auth. */
export const storage = AsyncStorage;
export const supabaseStorage = AsyncStorage;
